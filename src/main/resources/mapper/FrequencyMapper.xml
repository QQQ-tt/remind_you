<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.health.remind.mapper.FrequencyMapper">

    <select id="selectPageFrequency" resultType="com.health.remind.pojo.vo.FrequencyVO">
        select id,
               frequency_name,
               frequency_code,
               frequency_desc,
               frequency_cycle,
               frequency_number,
               type,
               status
        from frequency ${ew.customSqlSegment}
    </select>
    <select id="selectListFrequency" resultType="com.health.remind.pojo.vo.FrequencyVO">
        select id,
               frequency_name,
               frequency_code,
               frequency_desc,
               frequency_cycle,
               frequency_number,
               type,
               status
        from frequency ${ew.customSqlSegment}
    </select>
</mapper>
